import ForwardToInboxIcon from '@mui/icons-material/ForwardToInbox';
import React from "react";
import {styled} from "@mui/material";

export const StyledAppliedJobsIcon = styled(ForwardToInboxIcon)(() => ({
    fontSize: '2em',
    color: '#666',
    marginRight: '1rem'
}))