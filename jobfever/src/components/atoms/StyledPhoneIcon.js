import {ContactPhone as MUIContactPhoneIcon} from '@mui/icons-material';
import React from "react";
import {styled} from "@mui/material";

export const StyledContactPhoneIcon = styled(MUIContactPhoneIcon)(() => ({
    fontSize: '1.5em',
    color: '#666',
    marginRight: '1rem'
}))
