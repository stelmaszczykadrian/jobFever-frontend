import { Check as MUICheckIcon } from '@mui/icons-material'
import React from "react";
import {styled} from "@mui/material";

export const StyledCheckIcon = styled(MUICheckIcon)(() => ({
    fontSize: '1.5em',
    color: 'rgb(183, 4, 11)',
}))