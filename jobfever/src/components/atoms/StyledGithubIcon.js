import {GitHub as MUIGitHubIcon} from '@mui/icons-material';
import {styled} from "@mui/material";

export const StyledGithubIcon = styled(MUIGitHubIcon)(() => ({
    fontSize: '1.5em',
    color: '#666',
    marginRight: '1rem'
}))
