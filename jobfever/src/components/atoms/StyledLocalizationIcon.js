import PlaceIcon from '@mui/icons-material/Place';
import {styled} from "@mui/material";

export const StyledLocalizationIcon = styled(PlaceIcon)(() => ({
    fontSize: '3em',
    color: 'rgb(183, 4, 11)',
    marginRight: '3%'
}))