import { Work as MUIWorkIcon } from '@mui/icons-material'
import React from "react";
import {styled} from "@mui/material";

export const StyledWorkIcon = styled(MUIWorkIcon)(() => ({
    fontSize: '2em',
    color: '#666',
    marginRight: '1rem'
}))