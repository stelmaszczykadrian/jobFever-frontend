import { Delete as MUIDeleteIcon } from '@mui/icons-material'
import React from "react";
import {styled} from "@mui/material";

export const StyledDeleteIcon = styled(MUIDeleteIcon)(() => ({
    fontSize: '1.4em',
    color: 'rgb(183, 4, 11)',
}))