import {LinkedIn as MUILinkedInIcon} from '@mui/icons-material';
import {styled} from "@mui/material";

export const StyledLinkedInIcon = styled(MUILinkedInIcon)(() => ({
    fontSize: '1.5em',
    color: '#666',
    marginRight: '1rem'
}))
